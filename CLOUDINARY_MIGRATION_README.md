# Cloudinary migration note
# Run the following commands after this change:
#
# python manage.py makemigrations
# python manage.py migrate
#
# Then re-upload images via the Django admin to store them in Cloudinary.
